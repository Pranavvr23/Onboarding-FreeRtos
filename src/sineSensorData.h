#ifndef SINESENSORDATA_H
#define SINESENSORDATA_H

// The sineSensorData function is responsible for simulating sensor data based on a sine wave. The function generates a float value that oscillates smoothly between 0 and 100, simulating a sensor reading that varies over time in a predictable pattern.

float sineSensorData(void);

#endif // SINESENSORDATA_H