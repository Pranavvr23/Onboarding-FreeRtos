#ifndef RANDOMSENSORDATA_H
#define RANDOMSENSORDATA_H

// The randomSensorData function is responsible for simulating random noise in senosr data. The function generates a random float value between -20.0 and 20.0.

float randomSensorData(void);

#endif // RANDOMSENSORDATA_H